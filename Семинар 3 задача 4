# Напишите программу, которая будет преобразовывать десятичное число в двоичное. Нельзя использовать готовые функции.

n = int(input(' Введите число которое будем переводить в двоичную сс '))
str = []
c = 0
x = n
str2 = []
while n >= 1:                                               #выполняем деление на цело на 2 пока число не станет меньше
                                                            #либо равно 1 (как мы вручную делаем столбиком)
    a = 2 * (n // 2)                                        #"сохроняем" целое число которое делилось на цело
    str.append(n - a)                                       #записываем в строку остаток от деления
    n = n // 2

for i in range(len(str)):                                   #переворачиваем полученную после цикла while строку
    str2.append(str[(len(str)-1)-i])

def print_str(str2):                                        #печатаем цифры из строки "одним числом"
    for i in range(len(str2)):
        print(str2[i], end = '')

print(f' Число {x} в двоичной системе счисления = ')
print_str(str2)
