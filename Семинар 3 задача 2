# Напишите программу, которая найдёт произведение пар чисел списка.
# Парой считаем первый и последний элемент, второй и предпоследний и т.д.

from random import randint

n = int(input("Введите количество элементов строки "))
str = []
sum = 0
str2 = []
for i in range(n):
    str.append((randint(-10,10)))
print(str)

for i in range(n//2):
    str2.append(str[i]*str[(n-1)-i])

if n % 2 != 0:
    str2.append(str[i+1]*str[i+1]) #не понимаю как это сработало, написал методом тыка.
                                #объясните как он понимает число какого индекса брать (через str[n/2-0.5] не срабатывало

print(str2)


